#include "core.h"

std::vector<core::TileError> core::Validator::validate(const Canvas &, const BgPalettes &)
{
  std::vector<TileError> errors;
  // TODO: iterate over 8x8 tiles
  // check max 4 colors per tile
  // check palette assignment
  return errors;
}
